#ifndef MAIN_LOOP_H
#define MAIN_LOOP_H

// The application's main frame loop.  Runs continuously until the window
// should close.  Handles input, rendering, SSAA, text overlay, and
// per-frame profiling.
void imvw_main_loop(void);

#endif //MAIN_LOOP_H
