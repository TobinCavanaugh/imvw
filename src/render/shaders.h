#ifndef SHADERS_H
#define SHADERS_H

// Load (or reload) custom shaders from the pre-loaded sources in ctx.
// Called once at startup and on settings reload.
void load_custom_shaders(void);

#endif //SHADERS_H
